{
  "name": "graphapi-client",
  "version": "1.0.0",
  "description": "A client to make fetching from GraphApi easier",
  "scripts": {
    "build": "esbuild index.ts --bundle --outfile=dist/graphapi-client.min.js --minify --target=es2015 --format=iife --global-name=GraphApi"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "0.17.10",
    "nanographql": "^2.0.0",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@types/nanographql": "^2.0.1"
  }
}
